{
  "name": "sample-app",
  "version": "1.0.0",
  "description": "Sample Node.js app for ECS deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
